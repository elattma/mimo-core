import { IntegrationConfig } from "../model";

const uploadFileIntegrationConfig: IntegrationConfig = {
  id: "upload_file",
  name: "Upload a file",
  description: "Some description for file upload",
  auth_strategies: [],
};

export default uploadFileIntegrationConfig;
