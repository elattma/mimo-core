import { IntegrationConfig } from "../model";

const webLinkIntegrationConfig: IntegrationConfig = {
  id: "web_link",
  name: "Website",
  description: "Some description for website",
  auth_strategies: [],
};

export default webLinkIntegrationConfig;
